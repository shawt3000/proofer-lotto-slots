import React from 'react'
import ReactDOM from 'react-dom/client'
import ProoferSlots from './ProoferSlots'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ProoferSlots />
  </React.StrictMode>,
)